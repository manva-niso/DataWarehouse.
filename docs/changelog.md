# Changelog

(Coder and reviewer agents append one line per significant change here. Do not rewrite this file; only append.)

- Updated OpenCode Go routing to reserve scarce models for architecture, planning, and orchestration while assigning high-volume models to implementation.
- Added the rerunnable BigQuery schema and seeded the four required source dimension values; warehouse loads remain next in the build order.
- Added the integration registry and setup checklist for credentials, APIs, BigQuery, BI tools, Excel, and cross-module contracts.
- Added the Adzuna extractor (country allowlist, 429 retry honoring Retry-After, typed parse/config/API failures) and the shared raw BigQuery writer with an empty-payload guard; 11 tests pass.
- Added staging normalization and deduplication SQL, warehouse dimension and fact loads (idempotent MERGE upserts, disappeared postings never deleted), and skill-mention derivation from a seed dictionary.
- Added `mart_skill_demand_trend` and `mart_application_tracker` views.
- Added the Excel export and `mark_application` with validation and locked-file retry; 17 tests pass. Dashboard connection guide added under `dashboard/`.
- Review pass: fixed the Adzuna search parameter to the API's `what` key, and added the specified Python interfaces `load_dim_company`, `load_fact_job_posting`, and `refresh_mart_views` over the SQL files; 21 tests pass.
- Added Greenhouse, Lever, and Rippling extractors plus a shared HTTP helper (429 retry honoring Retry-After, parse-error isolation); Rippling detail calls are concurrency-limited to 5 and single-detail failures do not abort the run; 40 tests pass.
- Added staging data-quality checks, the pipeline orchestrator (watermark with 30-day default, run logging that always executes, exclusive file lock), SCD2 company versioning, and raw-table retention cleanup; 51 tests pass.
- Added the final mart views `mart_hiring_velocity` and `mart_company_activity`; all four mart views are now code-complete.
- Documented BigQuery Sandbox limitations and isolated dotenv loading in the credentials test; final verification: 51 tests pass.
- Initialized a uv project (pyproject.toml + uv.lock, venv in .venv): dependencies now tracked via uv add; all 51 tests pass under uv run pytest.
- Backend switch to Databricks Free Edition: added the `dbio/` package (databricks-sql-connector wrapper with run_sql, run_sql_script, query_rows, insert_rows), ported all SQL to Spark SQL / Delta dialect (get_json_object, MD5 ids, SEQUENCE/EXPLODE calendar, RLIKE skill matching, null-safe MERGE), and rewrote bigquery_io, loaders, refresh, data_quality_checks, export_tracker, and run_pipeline to use dbio; added orchestration/apply_schema.py (splits schema.sql per statement); 51 tests pass.
- Live Databricks verification (2026-08-12): Unity Catalog only (hive_metastore disabled), so DATABRICKS_CATALOG=workspace; discovered and fixed dotenv loading from project root and disabled driver telemetry; schema applied (15 statements), pipeline ran end-to-end (Adzuna 50 rows → 49 deduped postings → dims/facts/4 marts), DQ rules PASS, run logged PARTIAL (greenhouse token missing), second run confirmed idempotency, and a 49-row Excel export was written to exports/application_tracker.xlsx.
- Added Greenhouse/Lever/Rippling normalization branches, correct Greenhouse ISO and Lever epoch-millisecond date parsing, company enrichment from `config/companies.yaml`, and verified public Greenhouse boards (`figma`, `stripe`, `coinbase`) plus Lever boards (`leverdemo`, `palantir`). A live run loaded 50 Adzuna, 892 Greenhouse, and 697 Lever payloads; staging produced 1,601 postings and all DQ rules passed. Rippling remains supported but has no verified public board slug.
- Hardened Databricks batch inserts with multi-row, size-aware parameter chunks (the connector has a 1 MB parameter limit), added stale-lock recovery and visible pipeline stage progress; 57 tests pass.
- Activated Rippling with the confirmed public Gather board: migrated to the `ats.rippling.com/api/v2/board/{slug}/jobs` list/detail API, added pagination and verified field normalization, loaded one live Rippling posting, and logged the run successfully. Converted and wired the Databricks SCD2 company SQL and verified the 30-day raw retention cleanup; 58 tests pass.
- Added the dashboard specification with exact Databricks connection settings, four mart-based visuals, filters, field mappings, and acceptance checks. No Power BI/Tableau binary or fake screenshots were added because no desktop BI tool is installed in this environment.
- Installed Power BI Desktop for interactive report creation and added a reproducible `matplotlib` mart-preview generator; four verified PNG visuals now live under `docs/screenshots/`. The native report remains a desktop-saved artifact because Power BI closed before a `.pbix` could be saved.
- Added a full `README.md`, restored `PROJECT.md` as a technical project guide, and added `docs/outputs-guide.md` covering the data layers, every output table, SQL/Python consumption examples, Excel reuse, BI connections, refresh commands, and security rules.
- Phase 2 implementation & Streamlit overhaul: Applied 22 warehouse schema statements, seeded `user_profile` table, added 7 new specialized marts (`mart_job_match`, `mart_role_market`, `mart_posting_seasonality`, `mart_salary_summary`, `mart_source_quality`, `mart_application_funnel`, `mart_posting_longevity`), bringing total marts to 11.
- Fixed Streamlit multi-page module resolution: Injected root path resolution in `app/Home.py` and `app/pages/*.py` resolving `ModuleNotFoundError`. Backfilled `role_family` across warehouse facts (`fact_job_posting`), exposed `is_likely_closed` in `mart_job_match` and `mart_application_tracker` with 7-day retention policy, and built rich multi-factor filtering (role family, company, location, skills multi-select, salary floor, recency, open vs closed toggle).
- Added on-demand pipeline ingestion (`trigger_reload_pipeline`) and stale posting archival (`trigger_archive`) directly in Streamlit UI and CLI; 77 tests pass.
- Fixed Databricks ambiguous column error in Browse Opportunities (`1_🔍_Browse.py`): Qualified all filter predicate columns with table alias `t.` to resolve ambiguity with joined `job_posting_detail` (`d.posting_id`), refreshed `is_likely_closed` null-safety in `mart_job_match.sql`, and touched `last_seen_at` on active warehouse postings; verified live query execution returning 1,602 active opportunities.
- Transitioned application to a pure informative career intelligence platform: Executed Phase 2 staging & facts MERGE, populating `job_posting_detail` with 1,602 verified `posting_url` and `apply_url` links plus complete descriptions. Removed artificial application submission/status forms from Browse and Matches cards, replacing them with prominent direct "Apply on Career Page" link buttons, personal bookmarks ("⭐ Bookmark"), private research notes, and updated Tracker to "Saved Opportunities & Notes".
- Resolved `DELTA_MULTIPLE_SOURCE_ROW_MATCHING_TARGET_ROW_IN_MERGE` on pipeline reload: Hardened `staging/02_deduplicate.sql` with a windowed `ROW_NUMBER() OVER (PARTITION BY posting_id ORDER BY last_seen_at DESC, first_seen_at DESC)` CTE prior to `dedup_key` grouping, and guarded MERGE source queries in `warehouse/load_facts.sql` and `warehouse/load_dimensions.sql` to strictly eliminate duplicate source keys caused by ATS metadata title/location updates across crawls; verified zero duplicates across staging, facts, and detail tables with 1,961 active jobs, 100% URL coverage, and 77/77 tests passing.
- Fixed archive visibility and action notification persistence: Removed legacy `applications` filter restriction in `marts/mart_application_tracker.sql`, allowing all 649 archived postings in `fact_job_posting_archive` to be viewed under the "Archived / Closed Only" filter. Updated `Browse.py` to persist action feedback across Streamlit reruns via `st.session_state` and added real-time warehouse inventory stats (1,312 active, 649 archived, 1,961 total).
- Implemented cold-storage archival encoder & database purge (`orchestration/archive_encoder.py`): Stale postings are now serialized with full metadata, timestamps, role categories, and verification tokens into a compressed archive (`exports/archived_postings_encoded.jsonl.gz`) and completely purged from the Databricks Delta warehouse (`fact_job_posting_archive` = 0 rows), keeping the live database 100% lean and fast.
- Implemented job requirements parsing for freshers and experience criteria: Enriched `job_posting_detail` with `min_years_exp`, `max_years_exp`, `is_fresher`, and `experience_level` ('FRESHER', 'JUNIOR', 'MID', 'SENIOR', 'LEAD') via regex analysis in `warehouse/load_facts.sql`. Added multi-tier filtering in Streamlit (`Browse.py`) including a dedicated "🎓 Freshers / New Grads Only" toggle, experience level selector, experience slider, and prominent experience badges across Browse and Matches cards; 77 tests pass.
- Implemented continuous historical seasonality & domain release intelligence: Enhanced `marts/mart_posting_seasonality.sql` combining active facts (1,312) and historical archive facts (649) to compute seasonal release cycles (Winter, Spring, Summer, Fall), quarters (Q1-Q4), friendly domain classifications (`Data Engineering`, `Data Analytics`, `Data Science`, `Software Engineering`), and seasonal fresher hiring spikes. Overhauled the Seasonality tab in `app/pages/5_📊_Business_Analytics.py` with interactive domain filters, peak release season callouts, quarterly release wave charts, and fresher seasonal release trends.
- Added production containerization: Created multi-stage `Dockerfile` leveraging Astral `uv` for 10x faster builds, `docker-compose.yml` with healthchecks and persistent storage, `.dockerignore`, and `.env.example`.
- Created comprehensive enterprise `docs/SCALING_PLAN.md` outlining ingestion parallelization, Delta Lake Liquid Clustering, Redis query caching, and automated CI/CD.
- Enabled zero-local-dependency cloud deployment: Added `_get_setting` fallback in `dbio/databricks.py` and `ingestion/adzuna_extractor.py` to seamlessly read Databricks and API secrets directly from Streamlit Cloud Secrets (`st.secrets`).
- Created `docs/FREE_STREAMLIT_DEPLOYMENT_GUIDE.md`: Comprehensive, step-by-step guide for deploying the complete Job Market Pulse platform to Streamlit Community Cloud for 100% free hosting with zero local machine dependencies.
- Production containerization & deployment architecture: Added a multi-stage `Dockerfile` leveraging Astral `uv` for 10x faster builds, `docker-compose.yml` with healthchecks and persistent `/exports` volume mounts, `.dockerignore`, `.env.example`, a production deployment guide (`docs/deployment.md`), and a 5-phase enterprise scaling roadmap (`docs/SCALING_PLAN.md`) covering distributed ingestion, Delta Liquid Clustering, Mosaic AI vector search, and multi-tenant SaaS alerts.
- Expanded multi-source tracking & India hiring ecosystem: Built `ingestion/ashby_extractor.py` and integrated Ashby into `raw_ashby`, `dim_source`, staging normalization, and pipeline orchestrator with per-company fault tolerance. Expanded `config/companies.yaml` to 36 verified companies with rich industry, India locations (Bengaluru, Noida, Gurugram, Pune, Hyderabad, Remote India), and hiring pace metadata; verified live extraction across 6,000+ postings; 81 tests pass.
- Added Sarvam AI, Top Quant/HFT firms, and Enterprise & Fresher Hub: Added Sarvam AI (Ashby, 63 jobs), Tower Research Capital (85 jobs), Qube Research & Technologies (198 jobs), Graviton Research Capital (19 jobs), Zeta, Rubrik, Zscaler, and Clear to `config/companies.yaml` (44 ATS companies total with 7,000+ live jobs). Created `config/enterprise_portals.yaml` and `docs/ENTERPRISE_AND_CAMPUS_PORTALS.md` cataloging 84 enterprise companies (Google, Microsoft, Amazon, Meta, NVIDIA, Adobe, Flipkart, Swiggy, Deloitte), IT fresher graduate trainee programs (Infosys InStep, Wipro Elite, Accenture ASE, Cognizant GenC, Capgemini Exceller, LTIMindtree GET), and Fellowships/Hackathons (GSOC, MLH, SIH, Imagine Cup, DRDO). Added interactive Enterprise & Campus Hub tab into `app/pages/1_🔍_Browse.py`; 81 tests pass.



