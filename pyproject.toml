[project]
name = "codes"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "databricks-sql-connector>=4.4.0",
    "openpyxl>=3.1.5",
    "pandas>=3.0.5",
    "python-dotenv>=1.2.2",
    "pyyaml>=6.0.3",
    "requests>=2.34.2",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
